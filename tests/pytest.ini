[pytest]
# Live-integration suite for the vm CLI against the config's first Linux VM.
# Verbose, short tracebacks; tests are independent and may be run in any order.
addopts = -ra -v
testpaths = .
markers =
    offline: runs without the live VM (CI-safe); unmarked tests skip when the VM is unreachable
